# TASKS.md — Me Lembra Aí

> Backlog ordenado por prioridade. Marcar `[x]` ao concluir e registrar a sessão.

---

## Concluídas

| Task | Descrição | Sessão |
|---|---|---|
| TASK-01 | Firebase Auth: login, cadastro, recuperação de senha | 1–2 |
| TASK-02 | Onboarding 3 slides + flag `onboarding_visto` | 2 |
| TASK-03 | Seleção de 4 perfis (Idoso, Adulto, Filhos, Família) | 2 |
| TASK-04 | Telas de perfil: Idoso (TTS/STT/SOS/queda), Adulto, Criança, Família | 1–2 |
| TASK-05 | CRUD de lembretes no Firestore; correção da exclusão | 3 |
| TASK-06 | Notificações locais agendadas (única, diária, semanal) | 2 |
| TASK-07 | FCM: token salvo no Firestore; notificação local em foreground | 2 |
| TASK-08 | SOS: registro Firestore + chat automático + SosListenerService | 2–3 |
| TASK-09 | Build release: ProGuard Gson, pipeline fora do OneDrive | 3 |
| TASK-10 | Troca de perfil sem sair do app (BottomSheet em ConfigScreen) | 4 |
| TASK-11 | SOS push via VPS quando app encerrado (`sos_notifier.py`) | 5 |
| TASK-12 | Tela de histórico de confirmações de lembretes | 6 |
| TASK-13 | Widget Android de lembretes do dia (AppWidget 4×2) | 7 |
| TASK-14 | Modo escuro completo (ThemeData light + dark) | 8 |
| TASK-15 | 15 widget tests automatizados (login + create_reminder) | 9 |
| TASK-17 | SOS: ligação automática (CALL_PHONE + MethodChannel ACTION_CALL) | 10 |
| TASK-18 | SOS: canal `sos_alert` com fullScreen + vibração no familiar | 10 |
| TASK-19 | Chat áudio: segurar para gravar / soltar para enviar | 10 |
| TASK-20 | Maps: tela GoogleMap com posição atual e FAB SOS | 11 |
| TASK-21 | Foreground Service "Modo proteção ativo" (SosProtectionService.kt) | 11 |
| TASK-22 | SOS por teclas: 5 × volume em ≤ 3 s (MainActivity.onKeyDown) | 11 |
| TASK-23 | Configurar chave Google Maps API no AndroidManifest | 12 |
| TASK-25 | Countdown 5 s cancelável antes de disparar SOS | 12 |
| TASK-27 | Múltiplos contatos SOS (até 3); dialog para contatos adicionais | 12 |
| TASK-24 | Histórico SOS com status "visualizado por N familiares" | 12 |
| TASK-26 | SOS por 5 toques rápidos na tela (substitui Accessibility Service) | 12 |
| TASK-28 | Feature Veículos ativada: adulto (card dashboard) + idoso (botão) | 12 |
| TASK-29 | Certificado HTTPS do backend de IA (`certbot --nginx -d api.melbrai.com.br`, rodado pelo usuário via SSH) — `curl https://api.melbrai.com.br/healthz` de fora confirma `{"status":"ok"}` sem erro de TLS | 19 |
| TASK-31 | Chat Familiar: áudio não gravava (gesto segurar/soltar exigia precisão demais) — trocado para toque único; upload e reprodução confirmados funcionando de ponta a ponta no aparelho físico. Também corrigida a responsividade do botão de play (alvo de toque pequeno + sem feedback imediato ao tocar) | 21 |
| TASK-30 | Confirmado "Falar Comando" ponta a ponta com a IA — achada e corrigida a causa raiz real: a Groq descontinuou o modelo `llama-3.3-70b-versatile` (404 model_not_found), então a IA nunca respondia de fato, mesmo com certificado e rede OK. Trocado para `openai/gpt-oss-20b` (código + `.env` da VPS) | 21 |
| TASK-32 | "Falar Comando" evoluído pra assistente conversacional multi-turno: várias trocas seguidas sem tocar o botão de novo, nova ação `perguntar` (pede esclarecimento em vez de inventar dado), contexto real dos lembretes enviado à IA (grounding, incluindo itens da lista de compras), histórico limitado a 3 trocas, temperatura baixa, limite de 6 turnos por conversa, encerramento por frase ("obrigado"/"pode parar") ou silêncio. Testado ao vivo no aparelho físico | 21 |
| TASK-33 | Bug achado em teste ao vivo: dizer "SOCORRO" às vezes fazia o app falar "não entendi" **e** disparar o SOS ao mesmo tempo — causa raiz: o STT reporta status "done" (texto ainda vazio) um instante antes de entregar o resultado reconhecido, então `_processarComando('')` rodava e falava "não entendi" antes do evento com "SOCORRO" chegar e acionar o SOS de verdade. Corrigido com uma espera de 400 ms + checagem de `_sosDisparado` antes de falar "não entendi" — sem tocar na lógica/timing do SOCORRO em si. Confirmado corrigido pelo usuário no aparelho físico | 21 |
| TASK-34 | Detector de queda não funcionava com o app fechado, mesmo com "Modo Proteção" ativo — causa raiz: `SosProtectionService.kt` (Foreground Service nativo) não tinha nenhuma ligação com o `FlutterEngine`, que é destruído junto com a Activity quando o app fecha (comportamento padrão do `FlutterActivity`). Corrigido criando um `FlutterEngine` headless dentro do próprio serviço, rodando um novo entrypoint Dart (`fallDetectorEntrypoint`). Canal "call" extraído pra `CallChannel.kt` compartilhado. Confirmado no aparelho físico que o engine nasce e sobrevive ao fechamento do app, sem crash — disparo real da queda não testável neste aparelho (sem acelerômetro). SOS por 5 toques de volume confirmado como limitação permanente (precisaria de Accessibility Service, decisão de não implementar) | 22 |
| MOLLY-01 | Auditoria pré-implementação da MOLLY (assistente pessoal inteligente) — mapeados os serviços reaproveitáveis, gaps (sem telefone por familiar, sem tool-calling nativo, sem infraestrutura de teste de voz) e riscos (SOS não pode perder garantias, catálogo de modelos Groq instável, pasta órfã `me_lembra_ai/`). Produzido `docs/MOLLY_ARCHITECTURE_ANALYSIS.md` com plano de migração por fases. Nenhum código alterado | 23 |
| MOLLY-02 | Núcleo da MOLLY: criado `lib/features/molly/services/molly_agent_service.dart` + modelos `molly_message.dart`/`molly_tool_result.dart`. Porta (sem remover) a lógica hoje embutida em `elderly_screen.dart` (`_lerLembretesDoDia`, `_falarResumoAlertas`, `_adicionarItensNaLista`, `_criarLembreteDeAcao`/`_salvarLembrete`) para um serviço estático, sem estado, decoplado de TTS (devolve `MollyToolResult` em vez de chamar `VoiceService` direto). Reaproveita `AiCommandService`/`ReminderService`/`SosFeedService`/`ProfileService`/`NotificationService` já existentes — nenhuma chamada nova ao Firestore. `elderly_screen.dart` não foi tocado nesta tarefa (a troca da lógica inline por este serviço é um passo futuro deliberado, testado no aparelho físico). `flutter analyze` sem novos problemas; `flutter test` sem regressão (16 passando / 6 falhando, igual ao estado pré-existente) | 23 |
| MOLLY-03 | Registro de ferramentas da MOLLY: criado `lib/features/molly/services/molly_tool_registry.dart` + `models/molly_risk_level.dart`/`molly_tool_definition.dart` + `tools/{reminder_tool,family_tool,location_tool,profile_tool}.dart`, com o catálogo inicial pedido (createReminder, getTodayReminders, getTomorrowReminders, updateReminder, deleteReminder, confirmReminder, getReminderHistory, getFamilyMembers, sendFamilyMessage, callFamilyMember, getCurrentLocation, getUserProfile) — cada ferramenta com nome/descrição/parâmetros/validação/nível de risco (LOW/MEDIUM/CRITICAL, metadado only — a política de confirmação por risco fica para a TAREFA 4) e função delegando a um serviço já existente. Gap da TAREFA 1 (sem telefone por familiar) resolvido como planejado: `callFamilyMember` avisa pelo chat em vez de discar, mas continua CRITICAL. `molly_agent_service.dart` refatorado para usar o registro em `criar_lembrete`/`ouvir_lembretes`, removendo a duplicação com a TAREFA 2. `flutter analyze` limpo (zero problemas no módulo, mesmos 51 pré-existentes no resto do projeto); `flutter test` sem regressão (16/6, igual antes) | 23 |
| MOLLY-04 | Classificação de risco → política de execução: criado `lib/features/molly/services/molly_risk_policy.dart` (`MollyRiskPolicy.executar`), que aplica as três regras do prompt mestre sobre o `MollyRiskLevel` de cada ferramenta do registro — LOW roda direto; MEDIUM roda direto a menos que `ambiguo: true`; CRITICAL sempre pede confirmação a menos que `emergenciaAutorizada: true` (reservado para um gatilho de emergência legítimo futuro, nunca o SOS por voz, que continua 100% fora deste caminho). Adicionado `MollyToolResult.precisaConfirmacao`/`.confirmar()` (distinto de `precisaEsclarecimento`: aqui a intenção já está clara, só falta o "sim") e `MollyRiskPolicy.confirmarEExecutar` para reexecutar com os mesmos parâmetros já resolvidos, sem voltar à IA. `molly_agent_service.dart` passou a chamar a política em vez do registro direto nas duas ações já dispatchadas. Novo `test/molly_risk_policy_test.dart` (4 testes, sem precisar de mocks de Firebase — o gate de confirmação nunca chega a tocar o serviço real) cobre: CRITICAL sem emergência pede confirmação sem executar; CRITICAL com emergência pula a confirmação; MEDIUM com ambiguidade pede confirmação; ferramenta inexistente falha sem lançar exceção. `flutter analyze` limpo; `flutter test` sem regressão (20 passando incluindo os 4 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-05 | Voz conversacional: criado `lib/features/molly/services/molly_voice_service.dart` (`MollyVoiceService`), extraindo a lógica de STT hoje embutida em `elderly_screen.dart` (`_iniciarEscuta`/`_finalizarComando`/`_falar`) para uma classe própria, instanciável (diferente dos demais serviços estáticos da MOLLY, por guardar o ciclo de vida real do `SpeechToText`). Estados expostos via `ValueNotifier<MollyVoiceState>` (idle/listening/thinking/speaking/error) para uma UI futura (TAREFA 6) observar. Reaproveita `speech_to_text` e o `VoiceService` (TTS) já existentes, sem reimplementar nenhum dos dois. Porta fielmente a correção da TASK-33 (espera de 400 ms antes de entregar transcrição vazia, pra não atropelar uma palavra crítica que chegue atrasada do plugin) — mas generalizada: quem chama decide o que fazer com cada trecho reconhecido via `aoOuvirParcial`, então a detecção de "SOCORRO" continua sendo responsabilidade de quem usa o serviço, nunca desta camada (regra explícita documentada no código). Interrupção de fala (`interromperFala`) exposta como primitiva, sem duplexação real ouvir+falar simultâneo — isso fica para a TAREFA 33 (LiveKit), decisão registrada em comentário. `elderly_screen.dart` não foi alterado. `flutter analyze` limpo; `flutter test` sem regressão (sem teste automatizado novo — plugins de voz não têm mock no projeto ainda, gap já documentado na Tarefa 1; testável de fato só na TAREFA 28 ou manualmente no aparelho físico) | 23 |
| MOLLY-06 | Tela principal da MOLLY: criado `lib/features/molly/screens/molly_screen.dart` + `widgets/listening_indicator.dart`, acessível para 60+ (botões grandes, alto contraste, poucas opções, feedback visual e sonoro, indicações "Estou ouvindo"/"Pensando"/"Falando"). Conecta de verdade `MollyVoiceService` (TAREFA 5) → `MollyAgentService`/`MollyRiskPolicy` (TAREFA 2-4): microfone escuta, "SOCORRO" é checado 100% local a qualquer momento (nunca passa pela IA, mesma garantia de `elderly_screen.dart`), texto vira ação via `MollyAgentService.processar()`, e uma ação que precisa de confirmação (TAREFA 4) ganha um turno extra de sim/não antes de `MollyRiskPolicy.confirmarEExecutar`. Lista de lembretes de hoje via `StreamBuilder` em cima de `ReminderService.stream()` (dado real, ao vivo). Botão SOS fixo fora da área rolável — nunca escondido, em nenhum estado da tela. Deliberadamente mais simples que `elderly_screen.dart`: só um turno de pergunta/resposta + um turno de confirmação, sem a máquina completa de conversa (vários turnos, coleta de dia/hora, limite de turnos) — isso fica para um controller futuro (TAREFA 7+). Rota `/molly` adicionada em `main.dart`, mas **nenhum botão existente navega pra lá** — tela aditiva, sem tocar `elderly_screen.dart` nem nenhum fluxo em produção. Novo `test/molly_screen_test.dart` (4 testes de estrutura, sem tocar o microfone — mesmo gap de mock de `speech_to_text` da Tarefa 5). `flutter analyze` limpo; `flutter test` sem regressão (24 passando incluindo os 4 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-07 | Memória de curto prazo: criado `lib/features/molly/memory/short_term_memory.dart` (`ShortTermMemory`), extraindo `_historicoConversa`/`_turnosConversa`/`_coletandoLembrete`/`_slotDia`/etc. de `elderly_screen.dart` para uma classe instanciável e testável (mesmo padrão de `MollyVoiceService`, TAREFA 5). Guarda duas coisas com o mesmo ciclo de vida: (1) histórico de trocas para dar continuidade a perguntas em linguagem natural ("quando é minha consulta?" → "me lembra duas horas antes"), limitado a 3 trocas e exposto como `historicoParaIA`; (2) slots de dia/hora para coleta determinística de lembrete, deliberadamente fora do histórico mandado à IA (achado da sessão 22: o modelo esquece dado estruturado entre turnos). `MollyAgentService.processar()` passou a receber `ShortTermMemory? memoria` em vez de um `List<MollyMessage>` avulso — usa `memoria.historicoParaIA` e registra a troca sozinho ao final (`registrarTroca`), e marca `memoria.coletandoLembrete = true` quando a IA responde `perguntar` (preparação para a TAREFA 17, ainda não consumida por nenhum controller). `molly_screen.dart` (TAREFA 6) ganhou uma instância de `ShortTermMemory` por visita à tela — tocar o microfone de novo continua a mesma conversa em vez de começar do zero a cada toque — e fala um aviso + encerra a memória ao atingir o limite de 6 turnos. Documentado no código: a coleta local de dia/hora ainda não está de fato ligada nesta tela (falta extrair os parsers de `elderly_screen.dart`, TAREFA 17) — por ora, se a IA pedir esclarecimento, a tela só fala a pergunta e volta a chamar a IA no próximo toque. Novo `test/short_term_memory_test.dart` (6 testes, 100% puro Dart, sem Firebase/plugins) cobrindo limite de turnos, primeira fala, limite de trocas no histórico, merge de slots sem sobrescrever, e reset completo. `flutter analyze` limpo; `flutter test` sem regressão (30 passando incluindo os 6 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-08 | Memória de longo prazo: criado `lib/features/molly/memory/long_term_memory.dart` (`LongTermMemoryService`) + modelo `models/molly_memory_entry.dart`, persistindo em `users/{uid}/molly_memory/{id}` com os campos exatos pedidos (`type`, `value`, `source`, `createdAt`, `updatedAt`, `confidence`, `userApproved`). Regra central do prompt mestre — "nunca salvar informação sensível automaticamente sem autorização" — reforçada em duas camadas: (1) `salvar()` recusa gravar se `userApproved` não vier `true` explicitamente (sem valor padrão que permita esquecer); (2) mesmo assim, confere `SettingsService.getMollyMemoriaAutorizada()`, um interruptor geral novo (`cfg_molly_memoria_autorizada`, padrão `false` — opt-in) que corresponde ao "A MOLLY pode lembrar minhas preferências? SIM/NÃO" da TAREFA 9. `firestore.rules` atualizado: `molly_memory` é **privada ao dono**, diferente de `reminders` — família não lê, decisão já registrada na Tarefa 1 (risco 7). CRUD completo: `salvar`, `stream`/`getAll`, `atualizarValor` (edição não passa pela checagem de autorização — editar algo já aprovado é diferente de criar sem consentimento), `excluir`, `excluirTudo`. Nada disso foi ligado ao fluxo de conversa ainda (sem gatilho automático de "a Molly quer lembrar algo") nem tem tela própria — isso é a TAREFA 9. Alteração em `firestore.rules` implantada em produção nesta mesma sessão (`firebase deploy --only firestore:rules --project me-lembra-ai-bf0f0`, com autorização explícita do usuário). Sem teste automatizado novo — mesmo gap de Firebase de `ReminderService`/`FamilyService`/etc., nenhum desses tem testes unitários no projeto hoje. `flutter analyze` limpo; `flutter test` sem regressão (30/6, sem mudança na contagem) | 23 |
| MOLLY-09 | Tela "O que a Molly lembra": criado `lib/features/molly/screens/molly_memory_screen.dart`, único ponto de contato do usuário com a memória de longo prazo (TAREFA 8) — visualizar (lista ao vivo via `StreamBuilder`), editar valor (dialog), excluir uma por uma (com confirmação), apagar tudo (com confirmação, irreversível), e o interruptor "A Molly pode lembrar minhas preferências?" (`SettingsService.getMollyMemoriaAutorizada`/`setMollyMemoriaAutorizada`) que também cumpre "impedir novas memórias" — desligado, reforça a checagem que já existia em `LongTermMemoryService.salvar()` desde a TAREFA 8; memórias já guardadas continuam visíveis/editáveis até serem excluídas. Privacidade transparente: cada card mostra tipo, valor, data de atualização e origem (`source`); memórias com `confidence < 0.7` ganham um destaque visual ("Baixa confiança — confira se está certo"), cumprindo o que já estava planejado no comentário de `MollyMemoryEntry.confidence` desde a Tarefa 8. Rota `/molly-memory` adicionada em `main.dart`, aditiva como `/molly` — nenhum botão navega pra lá ainda. Novo `test/molly_memory_screen_test.dart` (4 testes, incluindo um de ponta a ponta: ligar o interruptor na tela e confirmar a persistência via `SettingsService`). `flutter analyze` limpo; `flutter test` sem regressão (34 passando incluindo os 4 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-10 | Contexto do usuário: criado `lib/features/molly/services/molly_context_service.dart` (`MollyContextService.montar()`) + modelo `models/molly_user_context.dart`, agregando nome/perfil (`ProfileService`), próximos lembretes e histórico recente confirmado (`ReminderService`, resumidos — nunca o documento completo), familiares (`FamilyService`, só nomes), configurações relevantes (SOS/chat/notificações ativos), horário atual, preferências (`LongTermMemoryService`, só se `SettingsService.getMollyMemoriaAutorizada()` — reforça o mesmo portão de consentimento da TAREFA 8/9 numa terceira camada) e o estado da conversa atual (turnos e `coletandoLembrete`, de uma `ShortTermMemory` opcional). Regra central da tarefa cumprida por design: `MollyUserContext.lembretesParaIA` continua sendo o único subconjunto de fato mandado ao backend de IA (o contrato de `AiCommandService`/`app.py` não mudou nesta tarefa — ampliar isso é decisão futura, ligada à TAREFA 32); o resto do contexto fica disponível para outros consumidores do app sem duplicar a lógica de busca em cada um. Absorveu `MollyAgentService.contextoPadraoDeLembretes()` (agora removido, redundante) e `molly_screen.dart` foi atualizado para chamar o novo serviço. **Achado de infraestrutura de teste**: uma tentativa de testar `MollyContextService.montar()` esbarrou num problema novo — diferente do gap já conhecido de `Firebase.initializeApp()` (coberto por `firebase_core_mocks.dart`), o primeiro acesso a `FirebaseAuth.instance` num processo de teste dispara `registerIdTokenListener`/`registerAuthStateListener` via platform channel, que não têm mock neste projeto, gerando uma falha assíncrona ruidosa que pode "vazar" pra outro teste do mesmo arquivo. Teste removido (não incluído no repositório) em vez de aceitar instabilidade na suíte — mesmo princípio já aplicado a `ReminderService`/`FamilyService`/etc. Registrado para quando a TAREFA 28 avaliar mocks de Firebase mais completos. `flutter analyze` limpo; `flutter test` sem regressão (34/6, sem mudança na contagem) | 23 |
| MOLLY-11 | Personalidade da MOLLY: criado `lib/features/molly/services/molly_prompt_service.dart` com a descrição formal da personalidade (paciente, educada, simples, gentil, objetiva, acolhedora; nunca infantiliza; nunca usa linguagem técnica; frases curtas) e uma checagem leve reutilizável (`contemLinguagemTecnica`) para conferir uma fala nova antes de shipar — não chamada automaticamente em nenhum fluxo, é uma ferramenta de revisão. **Auditoria real das falas já escritas nas Tarefas 2-10** encontrou duas violações concretas — "Reconhecimento de voz indisponível." em `molly_screen.dart` e `molly_voice_service.dart` — corrigidas para "Não consegui usar o microfone agora/neste aparelho.", seguindo à risca o exemplo "bom" do próprio prompt mestre (que já tinha sido usado ao pé da letra em `location_tool.dart` desde a Tarefa 3). A personalidade também foi aplicada onde ela de fato pesa mais — o `SYSTEM_PROMPT` real enviado à Groq em `server/ai_command_server/app.py`, que ganhou uma seção explícita de personalidade antes do contrato JSON. Implantado em produção nesta mesma sessão, com autorização explícita do usuário: chave SSH dedicada criada (`~/.ssh/melembra_vps`), `app.py` copiado via `scp` para `/root/ai_command_server/app.py`, `systemctl restart melembra-ai-backend`, `healthz` confirmado OK interno e externo (`https://api.melbrai.com.br/healthz`). **Nota de segurança**: a senha root da VPS foi colada em texto plano pelo usuário no chat durante o processo — usada uma única vez só para instalar a chave pública; recomendado trocá-la, já registrado em `docs/CURRENT_STATE.md`. Novo `test/molly_prompt_service_test.dart` (4 testes, 100% puro Dart) cobrindo os exemplos ruim/bom do próprio prompt mestre e casos genéricos. `flutter analyze` limpo; `flutter test` sem regressão (38 passando incluindo os 4 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-12 | Respostas curtas: adicionado `MollyPromptService.resumoCurto<T>()` — gera fala em 1-3 frases a partir de uma lista, nunca lendo item por item (0 itens: vazio; 1: "Você tem 1 X: [descrição]."; 2: segue ao pé da letra o exemplo do prompt mestre "Você tem 2 X. Um é [...], e o outro é [...]."; 3+: quantidade + só o primeiro item, evitando a "lista extensa falada" que o prompt mestre pede pra evitar). **Violação real corrigida**: `ReminderTool._remindersDoDia()` (usada por `getTodayReminders`/`getTomorrowReminders`) gerava um `falasEmSequencia` com um item de TTS por lembrete — exatamente a "lista extensa falada" proibida quando havia muitos lembretes no dia; substituído por uma chamada a `resumoCurto`, uma única fala. A lista completa continua em `dados['lembretes']` pra exibição visual (a regra é sobre o que é FALADO, não sobre a tela). `SYSTEM_PROMPT` do backend (`app.py`) ganhou a regra explícita com o exemplo literal ("Nunca leia uma lista item por item..."), implantado em produção na mesma sessão com autorização do usuário (`scp` + `systemctl restart melembra-ai-backend`, sem senha desta vez — só a chave dedicada da TAREFA 11, `healthz` confirmado OK). Novo `test/molly_prompt_service_test.dart` ampliado com 4 testes de `resumoCurto` (0/1/2/3+ itens, incluindo o exemplo literal de 2 itens do prompt mestre). `flutter analyze` limpo; `flutter test` sem regressão (42 passando incluindo os 4 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-13 | Briefing matinal inteligente: criado `lib/features/molly/services/molly_briefing_service.dart` (`MollyBriefingService`), evoluindo o resumo matinal que hoje era só uma notificação Android genérica e fixa ("Confira seus lembretes de hoje", sem os lembretes de verdade). `gerar()`/`gerarDeLista()` montam a fala real no formato do exemplo literal do prompt mestre — saudação, contagem, até dois destaques nomeados por tipo ("Seu remédio"/"Sua consulta"), fechamento "Quer que eu leia tudo?"; com 3+ lembretes, só os dois primeiros são destacados (mesma disciplina da TAREFA 12 contra listas extensas faladas). **Bug pré-existente achado e corrigido** em `lib/services/notification_service.dart`: tocar na notificação matinal fazia `_onNotificationTap` capitalizar o payload cru e falar literalmente "Matinal!" (o payload da notificação é a string fixa `'matinal'`, nunca foi pensado pra ser falado) — corrigido pra chamar `MollyBriefingService.gerar()` nesse caso específico, mantendo intacto o comportamento antigo (capitalizar o payload) pra todo o resto (lembretes normais). Contexto opcional citado no prompt mestre para o futuro (previsão do tempo, aniversários, mensagens familiares) não incluído — só lembretes reais, única fonte de dado já confiável hoje. Novo `test/molly_briefing_service_test.dart` (5 testes, 100% puro Dart — `gerarDeLista` recebe a lista já pronta, não toca `FirebaseAuth`, mesmo cuidado da Tarefa 10) reproduzindo o exemplo literal da tarefa e casos de borda (0/1/3+ itens, tipo sem sujeito conhecido). `flutter analyze` limpo (os 2 avisos pré-existentes de `notification_service.dart` continuam lá, só com linha deslocada); `flutter test` sem regressão (47 passando incluindo os 5 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-14 | Comandos naturais: reforçada a regra "criar_lembrete" do `SYSTEM_PROMPT` (`app.py`) com as quatro frases de exemplo literais do prompt mestre citadas como sinônimos explícitos ("Me lembra do remédio às oito." / "Coloca um lembrete para oito horas." / "Às oito eu preciso tomar meu remédio." / "Não deixa eu esquecer o remédio às oito.") — few-shot pra reforçar que a IA (caminho principal de NLU hoje) já deve tratar variação de linguagem natural como a mesma intenção. Criado também `lib/features/molly/models/molly_intent.dart` (`MollyIntentHints.pareceCriarLembrete`) — um sinal local, determinístico e testável, reconhecendo as mesmas quatro frases e variações próximas via palavras-gatilho; documentado como base para a TAREFA 17 (`OfflineIntentService`), mas **não ligado a nenhuma decisão real ainda** — hoje `MollyAgentService` não tem nenhum caminho de criar lembrete sem IA (isso só existe no `elderly_screen.dart` antigo). Novo `test/molly_intent_test.dart` (3 testes, 100% puro Dart) reproduzindo as quatro frases literais + casos de não-correspondência (ouvir lembretes, adicionar na lista, SOCORRO, consultar alertas). Implantado em produção na mesma sessão, com autorização do usuário (`scp` + `systemctl restart melembra-ai-backend`, `healthz` OK interno e externo, arquivo remoto conferido idêntico ao local). `flutter analyze` limpo; `flutter test` sem regressão (50 passando incluindo os 3 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-15 | Modo Companhia: novo interruptor opcional `SettingsService.getMollyModoCompanhia()`/`setMollyModoCompanhia()` (padrão `false`, sem tela própria ainda — isso é a TAREFA 24). `MollyPromptService` ganhou a constante `modoCompanhia` com as regras de tom (calor humano, história curta permitida) e o guardrail central da tarefa: nunca substituir acompanhamento médico ou psicológico, e sugerir contato humano quando o usuário parecer triste/sozinho/em sofrimento, em vez de "resolver" sozinha. `SYSTEM_PROMPT` do backend (`app.py`) ganhou a mesma regra na ação `responder`, incluindo uma proteção extra: nunca dizer que vai ligar/mandar mensagem/avisar um familiar (ex.: "quero falar com minha filha") — isso ainda não é uma ação que a MOLLY executa (fica pra TAREFA 20); só conversar e sugerir que o próprio usuário entre em contato. `molly_intent.dart` ganhou `MollyIntentHints.pareceBuscarCompanhia()`, reconhecendo as cinco frases de exemplo do prompt mestre e variações — mesmo espírito de sinal local documentado/testado da TAREFA 14, não ligado a nenhuma decisão real ainda. `test/molly_intent_test.dart` ampliado com 3 testes novos. Implantado em produção na mesma sessão, com autorização do usuário (`scp` + `systemctl restart melembra-ai-backend`, `healthz` OK interno e externo). `flutter analyze` limpo; `flutter test` sem regressão (53 passando incluindo os 3 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-16 | Preparação da wake word "Molly": pesquisa de integração com Picovoice Porcupine documentada por extenso em `lib/features/molly/services/wake_word_service.dart` — achados principais: "Molly" não é uma palavra-chave pronta (exige treinar uma palavra CUSTOM no Picovoice Console, gerando um `.ppn` por plataforma), precisa de uma `AccessKey` própria, impacto de bateria não-zero (escuta contínua + rede neural leve, exigiria Foreground Service dedicado igual ao do detector de queda da sessão 22 pra sobreviver em segundo plano), indicador de microfone sempre visível no Android 12+/iOS, e licenciamento comercial a avaliar além do tier gratuito. Seguindo a orientação explícita da tarefa ("não integrar imediatamente... se houver impacto significativo de bateria"), entregue só a abstração `WakeWordService` (estado idle/listening/detected/unavailable/error, `iniciar`/`parar`/`dispose`) + a implementação `UnavailableWakeWordService` (sempre indisponível, nenhuma dependência nativa nova adicionada) + o feature flag `SettingsService.getWakeWordEnabled()`/`setWakeWordEnabled()` (padrão `false`) exatamente como a tarefa pede — nenhum pacote `porcupine_flutter`/`picovoice_flutter` adicionado ao `pubspec.yaml`, nenhuma `AccessKey`, nenhum modelo `.ppn`. `WakeWordService.criar()` já lê a flag mas sempre devolve a implementação indisponível hoje — pronta pra quando uma implementação real existir. Novo `test/wake_word_service_test.dart` (4 testes, 100% puro Dart) confirmando que o stub nunca reporta sucesso e que `criar()` devolve sempre a implementação indisponível, com a flag ligada ou desligada. `flutter analyze` limpo; `flutter test` sem regressão (57 passando incluindo os 4 novos / 6 falhando pré-existentes) | 23 |
| MOLLY-17 | Modo offline: criado `lib/features/molly/services/offline_intent_service.dart` (`OfflineIntentService`), reconhecendo localmente os cinco comandos críticos do exemplo do prompt mestre — "socorro" (segunda camada de segurança, dispara `SosService.trigger` direto; o caminho principal continua sendo a checagem ao vivo em `molly_screen.dart`), "meus lembretes" (via `getTodayReminders`), "ligar para X" (extrai o nome e vai por `callFamilyMember`, CRITICAL — sempre pede confirmação), "que horas são" (resposta 100% local, sem nenhum serviço), "confirmar remédio" (acha o remédio de hoje ainda não confirmado; se houver mais de um candidato, usa `ambiguo: true` — primeiro uso real desse parâmetro da TAREFA 4, até aqui adormecido). Classificação (`classificar()`) separada da execução (`tentar()`) de propósito: é pura, sem Firebase/IA, então testável sem esbarrar no problema de `FirebaseAuth` documentado na Tarefa 10. **Integração real, não só infraestrutura pronta**: `MollyAgentService.processar()` agora chama `OfflineIntentService.tentar()` **antes** de qualquer chamada à IA — exatamente o fluxo do prompt mestre (detecção local → reconhecida? → sim: executa local / não: tenta IA) — então esses cinco comandos funcionam mesmo com a Groq fora do catálogo de novo (já aconteceu duas vezes) ou sem internet. Regex de "ligar para X" cuidadosamente ancorada no início da frase e com guarda contra dígito/hora/"lembr", pra não atropelar um lembrete disfarçado de ligação (ex.: "não deixa eu esquecer de ligar pro médico às 8"). Novo `test/offline_intent_service_test.dart` (10 testes): os cinco comandos literais, variações de cada um, os dois casos de falso positivo evitados na extração de "ligar para", frases de outras intenções, e dois testes de `tentar()` (o caminho 100% local de "que horas" e uma frase não reconhecida devolvendo `null`). `flutter analyze` limpo; `flutter test` sem regressão (67 passando incluindo os 10 novos / 6 falhando pré-existentes). Sem mudança no backend — nada pra implantar na VPS | 23 |
| MOLLY-18 | SOS por voz: ampliado `OfflineIntentService` com um novo `OfflineIntent.possivelEmergencia`, reconhecendo as cinco frases mais suaves do exemplo do prompt mestre ("me ajuda", "chama minha filha", "chama alguém", "estou passando mal", "preciso de ajuda") — distintas de "SOCORRO" (que continua disparando o SOS na hora, sem esta etapa). `MollyToolResult` ganhou `precisaConfirmacaoDeEmergencia` (novo, deliberadamente separado de `precisaConfirmacao` da TAREFA 4 — são modalidades de confirmação diferentes: contagem regressiva visual com botão Cancelar, não um "sim/não" falado). `molly_screen.dart` ganhou `_confirmarPossivelEmergencia()`, reproduzindo o diálogo exato do exemplo ("Parece que você precisa de ajuda." / "Vou acionar seu contato de emergência." / Cancelar) com uma contagem regressiva cancelável — mesmo padrão do countdown do botão SOS manual já existente em `elderly_screen.dart` (TASK-25), mas escrito de novo aqui em vez de tocar naquele arquivo (fora do escopo desta sessão). Duração da contagem tornada configurável via `SettingsService.getSosCountdownSegundos()`/`setSosCountdownSegundos()` (padrão 5s, sem tela pra mudar ainda — TAREFA 24), exatamente como a tarefa pediu ("contagem curta configurável"). O disparo técnico em si continua indo 100% por `SosService.trigger()` — o LLM nunca decide isso sozinho (a detecção é local, via `OfflineIntentService`, nunca chega a passar pela IA). `test/offline_intent_service_test.dart` ampliado com 3 testes novos (as cinco frases suaves, prioridade de SOCORRO quando aparecem juntas, e que `tentar()` monta a confirmação de emergência sem tocar Firebase). `flutter analyze` limpo; `flutter test` sem regressão (70 passando incluindo os 3 novos / 6 falhando pré-existentes). Sem mudança no backend — nada pra implantar na VPS | 23 |
| MOLLY-19 | Prevenção de falsos positivos na detecção de emergência suave (TAREFA 18): `OfflineIntentService._pareceEmergenciaSuave` deixou de usar `String.contains` puro e ganhou três guardas. (1) Palavra inteira: nova `_indiceDaFraseInteira()` só casa a frase quando os caracteres vizinhos não são letras — corrige um bug real (não hipotético: "eles me ajudaram ontem" casava com "me ajuda" via `.contains`, já que "ajudaram" começa com "ajuda"); usa `\p{L}` (Unicode) em vez de `\b`/`\w` porque o motor de regex do Dart trata letras acentuadas como "alguém" como não-palavra por padrão. (2) Negação: `_precedidaDeNegacao()` suprime quando uma das até três palavras antes da frase é "não"/"nao"/"nunca"/"nem" (ex.: "não preciso de ajuda, já resolvi sozinho"). (3) Relato de passado: lista `_marcadoresPassado` ("ontem", "semana passada", "no passado" etc.) suprime quando a frase relata algo já resolvido (ex.: "ontem eu disse que preciso de ajuda, mas já passou") — o cenário citado no `CURRENT_STATE.md` como pendente desde a TAREFA 18. Nenhuma mudança de comportamento pras cinco frases literais sem negação/passado por perto — continuam disparando a confirmação visual normalmente. `test/offline_intent_service_test.dart` ganhou 4 testes novos cobrindo os três casos de falso positivo evitados e um caso positivo real que continua funcionando. `flutter analyze` limpo; `flutter test` sem regressão (74 passando incluindo os 4 novos / 6 falhando pré-existentes). Sem mudança no backend — nada pra implantar na VPS | 24 |
| MOLLY-20 | Coleta local de dia/hora ligada na Molly nova (pré-requisito pra trocar o "Falar Comando"): criado `lib/features/molly/services/molly_reminder_parser.dart` (`MollyReminderParser`), portando `_extrairData`/`_extrairHora`/`_extrairHoraDeResposta`/`_limparTitulo`/`_inferirTipo`/`_inferirRecorrencia` de `elderly_screen.dart` sem reescrever a lógica. `molly_screen.dart` passou a ignorar a pergunta bruta da IA quando ela pede esclarecimento de lembrete (`perguntar`) e, em vez disso, extrai localmente o que já dá pra saber e pergunta só o campo que falta ("Para qual dia?"/"Que horas?"), criando o lembrete via `MollyRiskPolicy.executar('createReminder', ...)` ao completar — mesma regra de `elderly_screen.dart` (achado da sessão 22: a IA esquece dado estruturado entre turnos). De brinde, corrigido um bug real achado em teste ao vivo: `MollyVoiceService` usava `estado.value`/`textoParcial.value` depois de `dispose()` quando a tela era fechada com uma fala em andamento (`ValueNotifier` já descartado, exceção não tratada) — `_descartado` guarda todas as escritas de estado agora. Botão "Falar Comando" trocado por "Assistente Molly" em `elderly_screen.dart`, navegando pra `/molly` (`_falarComando` mantido no código, sem uso, pra reverter fácil). `test/molly_reminder_parser_test.dart` novo (13 testes, incluindo uma limitação conhecida herdada do código original: `\b` não corta quando "amanhã"/"manhã" é a última palavra, por causa da acentuação — cosmético, não afeta a data/hora extraída). `flutter analyze` limpo; `flutter test` sem regressão (87 passando incluindo os 13 novos / 6 falhando pré-existentes). Sem mudança no backend | 24 |
| MOLLY-21 | Perfil "Vovô / Vovó" renomeado para "Melhor Idade" em toda a interface (tela de seleção, "Trocar perfil", "Sobre o App", AppBar da tela do idoso — antes fixa como "Modo Idoso"). `ProfileService.getProfile()`/`getNameForProfile()` normalizam o valor legado ao ler (Firestore e SharedPreferences) — contas já existentes com "Vovô / Vovó" salvo continuam roteando certo e achando o nome salvo, sem precisar escolher o perfil de novo (mesmo espírito da migração transparente já usada pro campo legado `cfg_sos_numero`). `Reminder.perfil` e o manual do usuário (`MANUAL_USUARIO.md`, exibido em "Sobre o App") atualizados pra consistência. `flutter analyze` limpo; `flutter test` sem regressão (87/6, sem teste novo — é troca de string/normalização, já coberta indiretamente pelos testes de tela existentes) | 24 |
| MOLLY-22 | Assistente acionado direto, sem navegar de tela: extraído `lib/features/molly/widgets/molly_assistant_panel.dart` (`MollyAssistantPanel`) de `molly_screen.dart` — toda a lógica de conversa/voz/risco/emergência movida pra lá sem duplicar nada; `molly_screen.dart` virou uma casca fina (AppBar + o painel, `mostrarLembretesHoje: true`). O botão "Assistente Molly" em `elderly_screen.dart` passou a abrir o painel como `showModalBottomSheet` (`MollyAssistantPanel.abrirComoAtalho`, `autoIniciar: true`, sem a lista "Hoje") em vez de `Navigator.pushNamed('/molly')` — a tela atual continua por baixo, intacta, e o microfone já começa a ouvir sozinho assim que o painel aparece, mesma sensação imediata do antigo "Falar Comando". De quebra, portadas as frases de encerramento ("obrigado"/"pode parar"/"tchau"...) de `elderly_screen.dart`, que faltavam na Molly nova — falam "Até logo!" e encerram sem contar como turno. Documentado com cuidado (achado ao revisar o próprio código antigo): o microfone **não** reabre sozinho "pra qualquer assunto" — só durante a coleta de dia/hora (MOLLY-20) — isso é o comportamento já validado em produção, não uma limitação nova. `flutter analyze` limpo; `flutter test` sem regressão (87/6) | 24 |
| MOLLY-23 | Gatilho de memória de longo prazo: a IA agora pode sugerir, junto de qualquer ação, um campo opcional `memoria: {tipo, valor, confianca}` quando o usuário compartilha uma preferência durável (ex.: "pode me chamar de Seu Antônio", "eu sempre almoço ao meio-dia") — nunca decide sozinha salvar. `ComandoAction` (cliente e servidor) ganhou os campos; `MollyAgentService.processar()` só repassa a proposta adiante (`MollyToolResult.comPropostaDeMemoria`) se `SettingsService.getMollyMemoriaAutorizada()` estiver ligado, senão descarta em silêncio sem perguntar nada. `MollyAssistantPanel._confirmarMemoria()` pergunta em voz alta ("Posso lembrar disso: ...?") e só chama `LongTermMemoryService.salvar()` depois de um "sim" — que ainda confere o mesmo interruptor de novo por conta própria (tripla checagem, nenhuma camada confia cegamente na anterior). `SYSTEM_PROMPT` do backend (`app.py`) ganhou a regra com exemplos bons/ruins de quando propor. Implantado em produção com autorização do usuário (`scp` + `systemctl restart melembra-ai-backend`, `healthz` OK). Novo `test/molly_memory_gatilho_test.dart` (6 testes, 100% puro Dart — cobre `ComandoAction.fromJson` e `MollyToolResult.comPropostaDeMemoria`, não `MollyAgentService.processar` por tocar `SettingsService`/Firebase). `flutter analyze` limpo; `flutter test` sem regressão (93 passando incluindo os 6 novos / 6 falhando pré-existentes) | 24 |
| MOLLY-24 | Proatividade (Fase 8 do prompt mestre): criado `lib/features/molly/services/molly_proactive_service.dart` (`MollyProactiveService`), avisando remédio atrasado sem o usuário precisar perguntar. Reaproveita `ReminderService.getAll()` já existente — nenhuma consulta nova ao Firestore, nenhum serviço nativo/segundo plano novo (isso já é resolvido, com bem mais esforço, pelo Foreground Service do detector de queda). `verificarRemedioAtrasado()` (toca Firebase) delega pra `gerarAviso()` (pura, testável), que considera atrasado um lembrete tipo Remedio/Tomar de hoje, não confirmado, com mais de 30 minutos de atraso — evita alarde por um atraso de poucos minutos. `MollyAssistantPanel` chama isso assim que o painel abre (tela cheia ou atalho rápido), fala o aviso ANTES de começar a ouvir (senão o início da fala do usuário se perderia por cima do aviso). Novo `test/molly_proactive_service_test.dart` (8 testes, 100% puro Dart, mesmo padrão de `MollyBriefingService.gerarDeLista` pra não esbarrar no problema de `FirebaseAuth` sem mock da Tarefa 10). `flutter analyze` limpo; `flutter test` sem regressão (101 passando incluindo os 8 novos / 6 falhando pré-existentes). Sem mudança no backend | 24 |

---

## Pendentes

### TASK-16 — Publicação na Play Store (track interno)
**Prioridade:** Baixa  
**Arquivos:** `android/app/build.gradle.kts`, keystore  
**Descrição:** Assinar APK com keystore de produção e enviar para o track interno do Google Play Console.  
**Aceite:** APK disponível no Google Play Console (track interno), instalável via link.

---


---

## Regra de conclusão

1. Mover a task da seção "Pendentes" para a tabela "Concluídas"
2. Anotar a sessão
3. Atualizar `CURRENT_STATE.md` com os arquivos alterados
4. Atualizar `ARCHITECTURE.md` se o fluxo ou a estrutura mudou
