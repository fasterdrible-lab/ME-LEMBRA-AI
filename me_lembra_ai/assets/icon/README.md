# App Icon

Place the app icon PNG file here as `app_icon.png`.

The icon should be at least 1024x1024 pixels in PNG format.

This file is used by `flutter_launcher_icons` to generate all platform-specific app icons.

To generate icons after adding `app_icon.png`, run:
```
flutter pub run flutter_launcher_icons
```
